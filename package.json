{
  "name": "clientspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w server && npm run build -w client",
    "deploy:netlify": "npx netlify-cli deploy --prod --filter @clientspace/client --create-site --site-name clientspace-app",
    "seed:admin": "npm run seed:admin -w server",
    "seed:demo": "npm run seed:demo -w server",
    "lint": "npm run lint -w client && npm run lint -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
