[build]
  # npm ci skips rolldown platform bindings in workspaces (npm/cli#4828)
  command = "npm install && npm run build -w client"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
